# Built artifacts — each oracle is compiled from its .c by the matching
# build_*.sh and is never committed. The golden vectors they produce ARE
# committed (../tval_vectors/, ../logfile_vectors/), so the Rust differentials
# run without a C compiler; rebuild an oracle only when regenerating them.
tval_oracle
logfile_oracle
*.o
